---
title: "多智能体系统：编排与协作"
sidebarTitle: "多智能体"
description: "设计并实现多智能体系统：Supervisor、分层与点对点拓扑，以及路由、委派与共享状态。"
---

单个 Agent 挂很多工具迟早会撞墙：上下文塞满、任务复杂度超出一轮规划能管的范围、调试也越来越痛苦。**多智能体**把工作拆给各管一块的专家 Agent。好处很实际——上下文更小、可并行、失败可隔离——但代价也真实存在。搞清拓扑、知道什么时候该用（或不该用），比堆 Agent 更重要。

---

## 常见拓扑

### 1. Supervisor / 编排者

一个 **supervisor** 接用户任务，决定调用哪个专家，把子任务交出去，再汇总结果。Worker 彼此不知道对方，协调全走 supervisor。

```text
User → Supervisor → [Research Agent | Coding Agent | Writing Agent] → Supervisor → User
```

**适合：** 子域边界清楚、需要统一路由入口的任务。

### 2. 分层（Hierarchical）

嵌套 supervisor 做更细的委派。顶层路由到中层，中层再管自己的专家池。

```text
User → Top Supervisor → [Research Supervisor → [Web Agent | DB Agent]]
                      → [Code Supervisor   → [Writer Agent | Tester Agent]]
```

**适合：** 企业级、扁平 supervisor 一次要「想」太多 Agent 的场景。

<Warning>
  分层系统调试成本高。每多一层就多延迟；顶层一次误路由，下游全错。先把扁平 supervisor 做稳，再有明确扩展理由时才加层级。
</Warning>

### 3. 点对点（Swarm）

没有中心协调者，Agent 直接通信。任一 Agent 可按当前上下文把控制权交给另一个。LangGraph 的 `langgraph-swarm` 用共享消息历史 \+ `handoff` 工具实现这种模式。

```text
Agent A ←→ Agent B ←→ Agent C  （任意移交）
```

**适合：** 执行中途所有权会自然换人的流程（例如分诊交给领域专家，再交回做最终排版）。

---

## 用 LangGraph 做 Supervisor

最实用的起点：supervisor 在 **研究 Agent** 和 **编码 Agent** 之间路由，再合成输出。

### 定义 Worker

每个 worker 是自己编译好的图（或简单 chain），supervisor 把它们当工具来调。

```python agents/multi_agent/workers.py
from langchain_openai import ChatOpenAI
from langchain_community.tools.tavily_search import TavilySearchResults
from langgraph.prebuilt import create_react_agent

llm = ChatOpenAI(model="gpt-4o", temperature=0)

research_agent = create_react_agent(
    llm,
    tools=[TavilySearchResults(max_results=5)],
    state_modifier="You are a research specialist. Find accurate, up-to-date information.",
)

from langchain_experimental.tools.python.tool import PythonREPLTool

coding_agent = create_react_agent(
    llm,
    tools=[PythonREPLTool()],
    state_modifier="You are a coding specialist. Write clean, well-commented Python code.",
)
```

### 用结构化输出做路由

Supervisor 用 **structured output** 给出确定路由，而不是自由文本。

```python agents/multi_agent/supervisor.py
from typing import Literal
from pydantic import BaseModel
from langchain_openai import ChatOpenAI

WORKERS = ["researcher", "coder", "FINISH"]

class RouteDecision(BaseModel):
    next: Literal["researcher", "coder", "FINISH"]
    reasoning: str

llm = ChatOpenAI(model="gpt-4o", temperature=0)
router_llm = llm.with_structured_output(RouteDecision)

SUPERVISOR_PROMPT = """You are a supervisor coordinating a research agent and a coding agent.
Given the conversation so far, decide which agent should act next, or FINISH if the task is complete.

Workers:
- researcher: finds information, searches the web, retrieves facts
- coder: writes and executes Python code, performs calculations
- FINISH: the task is fully complete and a final answer is ready

Always reason before deciding."""

def supervisor_node(state: dict) -> dict:
    messages = [{"role": "system", "content": SUPERVISOR_PROMPT}] + state["messages"]
    decision = router_llm.invoke(messages)
    return {"next": decision.next, "reasoning": decision.reasoning}
```

### 组装图

```python agents/multi_agent/graph.py
from typing import TypedDict, Annotated, Literal
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import add_messages

class SupervisorState(TypedDict):
    messages: Annotated[list, add_messages]
    next: str
    reasoning: str

def make_worker_node(agent, name: str):
    def node(state: SupervisorState) -> dict:
        result = agent.invoke({"messages": state["messages"]})
        last = result["messages"][-1]
        last.name = name
        return {"messages": [last]}
    return node

builder = StateGraph(SupervisorState)
builder.add_node("supervisor", supervisor_node)
builder.add_node("researcher", make_worker_node(research_agent, "researcher"))
builder.add_node("coder", make_worker_node(coding_agent, "coder"))

builder.add_edge(START, "supervisor")

def route(state: SupervisorState) -> str:
    return END if state["next"] == "FINISH" else state["next"]

builder.add_conditional_edges("supervisor", route)
builder.add_edge("researcher", "supervisor")
builder.add_edge("coder", "supervisor")

graph = builder.compile()
```

### 跑起来

```python agents/multi_agent/run.py
from langchain_core.messages import HumanMessage

result = graph.invoke({
    "messages": [HumanMessage(
        content="Research the latest Python version, then write a script that prints its release date."
    )],
    "next": "",
    "reasoning": "",
})

for message in result["messages"]:
    sender = getattr(message, "name", message.__class__.__name__)
    print(f"[{sender}]: {message.content[:200]}")
```

---

## 共享状态与通信

多智能体需要交换信息。LangGraph 里常见三种做法：

### 状态传递（推荐）

Supervisor 图的 `messages` 当共享记忆。Worker 把输出追加进列表，后续 worker 和 supervisor 都能看到完整上下文。

### 子图共享字段

Worker 是完整子图时，父子 schema 重叠的字段可以共享。

### 外部记忆（长期）

跨多次多智能体运行仍要保留的状态，写到外部存储，每次开始时再取回（如 Redis）。

---

## 实践注意

### 什么时候别上多智能体

复杂度是真的。先想清楚更简单的路够不够：

- **单 Agent \+ 更合适的工具集**：很多「看起来需要多智能体」的问题，一个提示写清楚的 Agent 就能搞定
- **单 Agent \+ 更长上下文/摘要/检索记忆**：瓶颈是上下文时，先别急着拆 Agent
- **批处理独立任务**：循环跑单 Agent，比先造一层编排更省事

<Warning>
  每多一次 Agent 跳转，延迟、Token 成本和失败面都放大。四个 Agent、每个成功率 95%，端到端大约只剩 81%——还没算网络和限流。只有真的需要专长分工或并行时再加 Agent。
</Warning>

### 怎么调试

- 给每条消息打上产出 Agent 的 `.name`
- 路由决策打 INFO，带上 `reasoning`
- 开发时用 `stream_mode="updates"` 看逐节点执行
- Worker 先单独测通，再挂进 supervisor

### 用 LangSmith 做可观测

LangSmith 能在一张图里看到节点、LLM 调用和工具调用。设好 `LANGCHAIN_TRACING_V2` 等环境变量后，invoke 会自动上报，方便复盘「为什么 supervisor 走了这条路由」。